<% content_for :title, "Nuevo Arriendo" %>

<div class="container mt-4">
  <h1 class="mb-4">Nuevo Arriendo</h1>

  <%= render "form", arriendo: @arriendo %>

  <div class="mt-3">
    <%= link_to "Volver a Arriendos", arriendos_path, class: 'btn btn-secondary' %>
  </div>
</div>
